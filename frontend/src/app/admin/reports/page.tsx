"use client";
import {useState} from "react";import {downloadBookingReport} from "../../../lib/admin-api";
export default function AdminReports(){const [status,setStatus]=useState("");const [downloading,setDownloading]=useState(false);async function download(){setDownloading(true);setStatus("");try{const blob=await downloadBookingReport();const url=URL.createObjectURL(blob);const link=document.createElement("a");link.href=url;link.download="eventra-bookings.csv";link.click();URL.revokeObjectURL(url);setStatus("Booking report downloaded from current PostgreSQL data.");}catch(e:any){setStatus(e.message||"Unable to generate report.");}finally{setDownloading(false);}}return <div><p className="text-sm font-bold uppercase tracking-[.16em] text-[#8B000F]">Administration</p><h1 className="mt-2 font-serif text-4xl font-bold text-[#5c1632]">Booking reports</h1><section className="mt-7 max-w-2xl rounded-2xl border border-[#f0ded8] bg-white p-6 shadow-sm"><h2 className="text-xl font-bold text-[#6b1839]">Current booking export</h2><p className="mt-3 text-slate-700">Download the current database-backed CSV for booking oversight. It includes booking reference, customer email, vendor, service, event date, status, and quoted amount. It does not include passwords, session data, or authentication tokens.</p><button disabled={downloading} onClick={download} className="mt-6 rounded-xl bg-[#8B000F] px-5 py-3 font-semibold text-white disabled:opacity-60">{downloading?"Preparing report…":"Download bookings CSV"}</button>{status&&<p role="status" className="mt-4 rounded-xl bg-[#fff7f3] p-4 text-slate-800">{status}</p>}</section></div>}
