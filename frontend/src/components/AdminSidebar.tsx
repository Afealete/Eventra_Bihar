"use client";
import Link from "next/link";
import { usePathname, useRouter } from "next/navigation";
import { useState } from "react";
import { logout } from "../lib/auth";
const items=[["Dashboard","/admin/dashboard"],["Vendor applications","/admin/vendors"],["Users","/admin/users"],["Bookings","/admin/bookings"],["Reports","/admin/reports"]] as const;
export default function AdminSidebar(){const pathname=usePathname();const router=useRouter();const [open,setOpen]=useState(false);const link=(href:string)=>`block rounded-xl px-4 py-3 text-sm font-semibold transition focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-white ${pathname===href?"bg-white text-[#8B000F]":"text-white hover:bg-white/15"}`;const signOut=async()=>{await logout();router.replace("/admin/login");};return <><button type="button" aria-label="Open admin navigation" aria-expanded={open} onClick={()=>setOpen(!open)} className="fixed left-4 top-4 z-50 rounded-lg bg-[#8B000F] p-2 text-white shadow-lg md:hidden">{open?"×":"☰"}</button>{open&&<button aria-label="Close admin navigation" onClick={()=>setOpen(false)} className="fixed inset-0 z-30 bg-black/40 md:hidden"/>}<aside className={`fixed inset-y-0 left-0 z-40 flex w-72 flex-col bg-[#72102b] px-4 py-6 text-white shadow-xl transition-transform md:static md:translate-x-0 ${open?"translate-x-0":"-translate-x-full"}`}><Link href="/admin/dashboard" onClick={()=>setOpen(false)} className="px-3 font-serif text-2xl font-bold">Eventra <span className="text-[#ffd1b3]">Admin</span></Link><p className="mt-2 px-3 text-sm text-white/75">Platform management</p><span className="mt-4 w-fit rounded-full bg-white/15 px-3 py-1 text-xs font-bold uppercase tracking-wider">Administrator</span><nav className="mt-7 flex flex-1 flex-col gap-2">{items.map(([label,href])=><Link key={href} href={href} onClick={()=>setOpen(false)} className={link(href)}>{label}</Link>)}</nav><div className="space-y-2 border-t border-white/15 pt-4"><Link href="/" className={link("/")}>View marketplace</Link><button type="button" onClick={signOut} className="w-full rounded-xl bg-white px-4 py-3 text-left text-sm font-bold text-[#8B000F]">Sign out</button></div></aside></>}
